import React from 'react'
import { Streaming } from '../types/streaming'

type Props = {
  id: number
}

const StreamingDetail = ({ id }: Props) => {
  return (
    <div>StreamingDetail {id} {id}</div>
  )
}

export default StreamingDetail
